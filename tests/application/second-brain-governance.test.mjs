import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
const migration=await readFile(new URL("../../supabase/migrations/20260813214942_second_brain_governance_and_retrieval.sql",import.meta.url),"utf8");const hardening=await readFile(new URL("../../supabase/migrations/20260813215502_harden_knowledge_governance_privileges.sql",import.meta.url),"utf8");const page=await readFile(new URL("../../app/workspace/knowledge/page.tsx",import.meta.url),"utf8");
test("knowledge relations enforce workspace integrity and append-only supersession",()=>{assert.match(migration,/foreign key \(source_knowledge_id,workspace_id\)/);assert.match(migration,/foreign key \(target_knowledge_id,workspace_id\)/);assert.match(migration,/verification_status='superseded'/);assert.match(migration,/Replacement created as candidate/)});
test("knowledge governance denies anonymous RPCs and direct mutation",()=>{assert.match(hardening,/revoke all on table public\.kos_knowledge_relations from anon,authenticated/);assert.match(hardening,/grant select on table public\.kos_knowledge_relations to authenticated/);for(const name of ["kos_search_knowledge","kos_record_knowledge_relation","kos_supersede_knowledge"])assert.match(hardening,new RegExp(`revoke all on function public\\.${name}`))});
test("knowledge review exposes retrieval contradiction and supersession controls",()=>{for(const label of ["Search knowledge","Record governed relationship","Create superseding revision","Contradiction and supersession ledger"])assert.match(page,new RegExp(label))});
