import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase-server";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createSupabaseServerClient();const {data:c}=await s.auth.getClaims();if(!c?.claims?.sub)return NextResponse.redirect(new URL("/",_.url));const {data:v}=await s.from("kos_document_versions").select("bucket_id,object_path").eq("document_id",id).order("version_number",{ascending:false}).limit(1).single();if(!v)return NextResponse.json({error:"Document version not found"},{status:404});const {data,error}=await s.storage.from(v.bucket_id).createSignedUrl(v.object_path,300);if(error||!data)return NextResponse.json({error:"Preview unavailable"},{status:404});return NextResponse.redirect(data.signedUrl)}
