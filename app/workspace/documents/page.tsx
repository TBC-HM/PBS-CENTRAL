import Link from "next/link";import {redirect} from "next/navigation";import {DocumentLibrary} from "@/components/document-library";import {createSupabaseServerClient} from "@/lib/supabase-server";
export default async function DocumentsPage(){const s=await createSupabaseServerClient();const{data:c}=await s.auth.getClaims();if(!c?.claims?.sub)redirect("/");const{data:ws}=await s.from("kos_workspaces").select("id,name").limit(1);if(!ws?.length)redirect("/workspace");const w=ws[0];const[{data:companies},{data:documents},{data:links},{data:helpers}]=await Promise.all([s.from("kos_companies").select("id,name").eq("workspace_id",w.id).order("name"),s.from("kos_documents").select("id,title,document_type,status,updated_at,versions:kos_document_versions(original_filename,extraction_status,version_number)").eq("workspace_id",w.id).order("updated_at",{ascending:false}),s.from("kos_document_links").select("document_id,resource_id").eq("workspace_id",w.id).eq("resource_type","company"),s.from("kos_document_derivatives").select("id,document_id,derivative_type,target_language,status,content").eq("workspace_id",w.id)]);const companyMap=new Map((links??[]).map(x=>[x.document_id,x.resource_id]));const normalized=(documents??[]).map(doc=>({...doc,version:[...(doc.versions??[])].sort((a,b)=>b.version_number-a.version_number)[0]??null,company_id:companyMap.get(doc.id)??null,helpers:(helpers??[]).filter(x=>x.document_id===doc.id)}));return <main className="entity-page"><nav><Link href="/workspace">← Workspace</Link><Link href="/workspace/upload">Upload files or folders</Link></nav><header><p className="eyebrow">Knowledge · Documents</p><h1>Company files</h1><p>Preview, download, file, summarize and translate every governed document.</p></header><DocumentLibrary workspaceId={w.id} companies={companies??[]} initialDocuments={normalized}/></main>}
